# URL where the FastAPI backend is running
VITE_API_URL=http://localhost:8000

# Google OAuth 2.0 Web client ID. This is public browser configuration,
# but it must match the client ID configured in the backend.
VITE_GOOGLE_CLIENT_ID=replace-with-your-google-client-id.apps.googleusercontent.com
